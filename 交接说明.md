# 四校教授创业扫描 · 换对话交接说明

> **当前状态一览（2026-09-04，以此为准；下面各节是按时间累积的改动记录，早期章节里被后来推翻的地方已标 ⚠️）**
> - 页面 = `QBFJ AP Network.html`，artifact 同一链接，`capabilities:{db:{}}`，sharing=owner（不能公开分享）。
> - 数据 = 文件里的 `DB0`（939 人）+ claude.ai db `profs` 覆盖层；有效集合 `DB` 运行时现算。覆盖层 2026-09-04 已全部合回 DB0 并清空（§二十）。
> - 状态四态 growth / founded / stealth / potential；`hot` 默认全不选、用户手标；`manual:1` = 人改过、脚本不得重算。
> - 视图：卡片（默认）/ 表格 / 网络图（学校 › 学院 › 研究所/AP 分层，机构默认不画）。点任何人 → 全屏卡片，可编辑。
> - 没有侧栏、没有抽屉、没有图例、没有顶部统计栏、没有介绍段——都是用户明确要求去掉的。
> - **维护方式（2026-09-04 定）：只在页面里手改，或贴新闻让 Claude 用 `db/merge_news.py` + `write_db` 合；不再从 md 名录批量导入。**`rebuild_db0.py` 已删（最后版本在提交 08a4937），`enrich_org.py` / `scrape_sjtu.py` 仅作辅助（§二十一）。交大自动化与感知学院已整院剔除（仅留王贺升）。
> - 贴新闻更新走 `db/merge_news.py` + `write_db`，不改源文件、不重发布；`hot` 永远不由脚本写。
> - 本地预览：`~/Desktop` 对预览进程不可读，要拷到 scratchpad 起 http.server（§16.5）。

## 〇、只想加一位教授？看这一节就够

打开 `QBFJ AP Network.html`，找到第一个 `<script>` 里的 **`const DB=[`**，在数组里加一个对象：

```js
{"n":"某某","sch":"s","dept":"人工智能学院","title":"副教授",
 "email":"xx@sjtu.edu.cn","home":"https://…",
 "research":"具身操作、触觉感知",
 "honor":"国家优青 · 原 DeepMind",
 "fld":"embodied","status":"potential","hot":1,"score":4,
 "why":"实验室已有两项成果转化，与宇树有联合项目",
 "note":"","src":"手动补录 2026-09",
 "cos":[]}
```

**改这一处就够了。** 公司节点、投资机构节点、连线、顶部八个数字、四个筛选器、
表格行、卡片、TSV 导出，全部是从 `DB` 现算的，没有第二处需要同步。

字段速查（`*` 必填，漏了会在浏览器控制台被「数据自检」点名，不会静默混进图里）：

| 字段 | 说明 |
|---|---|
| `n*` `sch*` | 姓名；学校 `s`交大 `f`复旦 `t`清华 `p`北大 |
| `dept*` `title*` | 院系 / 研究机构；职称 |
| `email` `home` | **只收院系官网公开的职务邮箱**，不收手机微信；个人主页 URL |
| `research*` | 研究方向 |
| `honor` | 头衔荣誉与关键履历 |
| `fld*` | `embodied` `ai` `chip` `bio` `energy` `other` |
| `status*` | `growth` 成长期（B 轮以后） · `founded` 创业项目（≤B 轮有融资新闻） · `stealth` 水下（有项目无融资新闻） · `potential` 待创业 |
| `hot` | `1` = 高潜力。**与 status 正交**，可叠加。默认全不选，由维护者手动标，脚本永远不写它 |
| `grp` `lead` | 研究所/中心（学院下一级）；该学院或研究所的院长/所长。网络图第三层按 `grp` 分 hub |
| `manual` | `1` = 人手改过的记录。页面保存时自动打上；脚本（`merge_news.py`）不动它的状态与高潜力，合回 DB0 时必须带上 |
| `score` | 创业可能性 1-5，已创业固定 5。本图自行评分 |
| `why` | 评分依据，**必须是事实**，不要写「该方向热门」 |
| `note` | 口径警告 / 冲突 / 待核实 |
| `src` | 数据来源与日期 |
| `cos[]` | `{name, role, born, stage, fin, inv[]}`；待创业者留 `[]` |

加完刷新页面，**打开控制台看有没有 `[数据自检]` 警告**。没有就成了。

一次加很多人时：公司名和机构名要跟已有写法对齐（`无问芯穹 Infinigence AI` 而不是
`无问芯穹`），否则图上会裂成两个点。归一逻辑在数据侧做过一轮，页面运行时不再做归并。

---

## 一、怎么让新对话「接着改」而不是「另开一个」

**关键点：新对话直接发布会创建一个新 artifact，旧链接不会更新。**
必须把原 URL 作为 `url` 参数传给 Artifact 工具，才会更新到同一个链接。

在新对话里这样开场即可（复制粘贴）：

> 继续打磨这个网页：https://claude.ai/code/artifact/211e3d3e-7d7f-4de5-adcb-1625832c1ff4
> 源码在 ~/Desktop/QBFJ\ AP\ Network/QBFJ\ AP\ Network.html，改完用 url 参数更新到同一个链接，不要新建 artifact。
> 另外不要挂实时监听。
> 先读 ~/Desktop/QBFJ\ AP\ Network/交接说明.md 了解已定的设计约束。

## 二、四个页面与链接

| 页面 | 链接 | 源码 |
|---|---|---|
| **清北复交 AP Network**（主要在改的这个） | https://claude.ai/code/artifact/211e3d3e-7d7f-4de5-adcb-1625832c1ff4 | `~/Desktop/QBFJ AP Network/QBFJ AP Network.html` |
| 交大 × 复旦图谱 | https://claude.ai/code/artifact/4cc735db-279e-4805-8a2c-864e41367d71 | `交大复旦图谱.html` |
| 清华教授创业管道 | https://claude.ai/code/artifact/470eed1d-c9c9-4baf-86e7-496f58c6d7bf | `清华管道.html` |
| 北大资本闭环 | https://claude.ai/code/artifact/57fc8abf-14f6-4ff4-9f31-febbc9b9e300 | `北大闭环.html` |

四份 md 扫描原稿在 `~/Desktop/`：
`上海交大教授AP扫描_20260828.md`、`复旦教授AP扫描_20260828.md`、
`清华教授AP扫描_20260829_核实版.md`、`北大教授AP扫描_20260829.md`

## 三、已定的设计约束（**别无意中推翻**）

这些不是随手选的，是跑过色觉校验后定下来的：

1. **只做亮色，没有暗色模式** —— 四院校色在暗色亮度带里，青和蓝连正常色觉的 ΔE 都只有 8.7（低于 15 底线），无解。这是**有意的单主题**，不是漏做。
2. **四校色已按明度重新分级**，不是原始院校色：
   - 交大红 `#AC312A`（中）· 复旦蓝 `#244D9E`（最深）· 清华紫 `#AD51A7`（中亮）· 北大青 `#2CA6C0`（最亮）
   - 三个冷色（蓝/紫/青）在红绿色觉下会塌向蓝，靠明度阶梯才把最差色对从 ΔE 4.9 拉到 **8.3 达标**。**改任何一个色值都要重跑校验。**
3. **投资机构 `#877A5E` 故意是低彩度沙色** —— 它们没有学校归属，读作「无色」是语义正确的。彩度校验那项「不达标」是有意的。曾试过深板岩色，和复旦蓝在正常色觉下只有 ΔE 12.9，不可用。
4. **全部节点为圆形**，学校/教授用院校色，机构用沙色；机构圆的大小 = 覆盖学校数 + 是否连续出手；通吃四校额外一圈外框。
5. **公司不单独成点**，折入教授卡片（点教授看全部公司/角色/融资/投资方）。
6. **四个学校节点锚定在一条水平轴上**（`SPINE` 常量），否则布局会塌成竖条。这是踩过坑的。

## 四、踩过的坑（改代码时注意）

- **画布退化尺寸会永久锁死**：面板隐藏或容器未布局时 `getBoundingClientRect()` 可能返回 0 或 2，一旦把 canvas 设成那个尺寸就再也不恢复。`resize()` 里有 `if(r.width<80||r.height<80)return;` 守卫，**别删**。
- 同理保留 `visibilitychange` + `IntersectionObserver` 兜底——页面在隐藏状态首次布局时 ResizeObserver 收不到帧。
- ~~`fit()` 的 `padR` 要给右侧详情面板留位（宽屏 382px）~~ ⚠️ 已无侧栏（§九），`padR()` 固定 22，图占满画布。
- 力导向参数（排斥力/重力/连线长度）改动后要看填充率，目标 x/y 都在 0.7 左右。

## 五、数据口径注意

- 只收录**教授本人在公司任实职**的项目为创业/成长期；「联合实验室挂名」「技术源头」「传闻未官宣」⚠️ 现在**不再丢弃**，归入 `stealth` 水下项目（§14.1）；校友创办仍不收（非教职）。
- 已知身份陷阱（勿按持股企业录）：杨耀东↔灵初智能（联合实验室）、王启宁↔大艾机器人（创始人是北航帅梅）、洪波↔博睿康（学术合作方）。
- 已知同名陷阱：张林峰（北大元培/深势科技 ≠ 交大AI学院助理教授）、张林峰、灵汐科技（类脑芯片 ≠ 骨科手术同名公司）。
- **未解决的跨校冲突**：谢涛的归属，北大官网列为北大讲席教授，清华计算机系名录里也有「谢涛」且是 2025.05 最新条目，两校不可能都对。
- 「产业资本 / 国资」是本图自行分类，非官方口径。

## 六、2026-08-29 这一轮加的东西（**同样别无意中推翻**）

只动了「四校关系网」这一页，四校色值、圆形编码、SPINE 轴、resize 退化守卫都没碰。

1. **缩放与平移**：滚轮以光标为锚点缩放，拖空白处平移，双击空白或按 `0` 回到全图；
   工具条有 `＋ − 重置视图` 三个按钮供触屏用。缩放范围 `fitScale*0.65 ~ fitScale*9`。
   —— 之前 `#net` 一直挂着 `cursor:grab` 却没有任何平移代码，是个空承诺。
2. **`fit()` 不再是唯一的视图来源**：新增 `userView` 标志。读者一旦缩放/平移，
   `resize()` 就保持他当前看的世界坐标中心，不再重新 fit；但仍会重算 `fitScale`
   （缩放上下限和标签阈值都依赖它）。切标签页回来不会再把视图打回原点。
3. **节点半径随缩放放大**：`zf()=min(2.2,max(1,(scale/fitScale)^0.45))`，统一乘数，
   不破坏「圆大小＝覆盖学校数」的相对编码。
4. **搜索框**：匹配教授名、备注、公司名、赛道、机构名。命中节点画虚线圈、其余压暗。
   搜公司会同时点亮创办教授**和它的投资方**（机构节点的 hay 里含它投过的公司名），
   这是刻意的——`智元机器人` 能一次带出闫维新 + 腾讯 + 交大母基金。回车跳到最佳匹配。
5. ⚠️ 面板已删（§九），跳转链接现在在全屏卡片里，逻辑不变：
   **面板内可跳转**：教授卡片里的投资方、机构卡片「投过」下面的创办教授、学校名，
   都是 `data-go` 链接。目标若被当前筛选藏住，`goTo()` 会自动复位筛选并同步 chip 状态。
6. **键盘可达**：canvas 有 `tabindex=0`，方向键在节点间按方向就近跳、`Enter/Esc`、`+ - 0`。
   之前整张图对键盘完全不可达。
7. ~~图例~~ —— 曾在 stage 左下角加过一块浮层图例，**用户明确要求去掉，已移除，别再加回来**。
   编码说明本来就在 footer 的文字里（院校色／沙色／圆大小），不需要重复。
   随之把 `fit()` 的 `padB` 从 96 降回 56。

### 这轮修掉的两个真 bug

- **窄屏（<1040px）详情面板整个被裁掉**：`.stage` 是 `height:800px` + `overflow:hidden`，
  媒体查询把 `.panel` 改成 `position:static` 后它落在 canvas 下方，直接被裁没了——
  手机上点节点毫无反应。现在 canvas 单独包了一层 `.cbox`，窄屏时 `.stage` 改
  `height:auto;overflow:visible`，`.cbox` 固定 520px，面板跟在下面。
  **`resize()` 现在量的是 `#cbox`，不是 `cv.parentElement`**（后者在窄屏下会把面板高度算进去）。
- **窄屏 `padB` 曾是 190**（给那个看不见的面板留的位），导致图整体贴着上边缘、下半区全空。
  现在统一 96，图是真的垂直居中的。

## 七、2026-09-03 这一轮：从「关系图」改成「教授库」

网络图的一切编码（四校色、圆形、SPINE 轴、resize 守卫）都没动。变的是数据层和视图层。

### 7.1 架构：`DB` 是唯一真相源

以前数据摊在四个平行结构里：`PROFS`（扁平六元组）、`INV`（公司→投资方）、
`FIN`（公司→融资口径）、外加节点构建代码。加一位教授要改三处，漏一处就不一致。

现在只有 `DB`：**一位教授一个对象，公司折在 `cos[]` 里，投资方折在每家公司里**。
派生层（`/* ===== 派生层 ===== */` 那一段）从 `DB` 现算出 `N`（节点）、`L`（边）、
`CO`（公司表）、机构节点、顶部数字。`PROFS` / `INV` / `FIN` 三个常量**已删除**。

配套加了 **数据自检**：`REQ` 里的必填字段缺失、或 `status=founded` 却没填公司，
都会在控制台 `console.warn("[数据自检] …")` 点名。加完数据记得看一眼控制台。

### 7.2 状态分类（就是筛选器里那四个 tag）

- `founded` **创业项目** —— 教授本人在公司任实职
- `stealth` **水下项目** —— 有产业关联但不算实职创业：联合实验室挂名、技术源头、
  学生创办、传闻未官宣、已被收购状态不明。**这是原来「不收录」的那批人的去处**，
  以前直接丢掉，现在留下来并标明原因（`note` 字段）
- `potential` **待创业** —— 尚未创业的候选
- `hot` **高潜力** —— 与上面三者正交的独立开关，可以叠加筛

网络图里教授圆的填充承载这个区分：**实心 = 已创业，带中心点 = 水下，空心 = 待创业**。
没有新增色相，四校色仍然只编码学校归属。

### 7.3 三视图

`网络图 / 表格 / 卡片` 共用同一套筛选条件（`matchProf()` 是三视图唯一的判定入口，
图那边多一个 `inFilter()` 因为图不把搜索当筛选、只当高亮）。

- **表格**：八列可点排序，`table-layout:fixed` + `<colgroup>` 定宽。
  **不要改回 auto 布局** —— auto 布局下浏览器会把可任意换行的中文列压成一字一行。
  ~~点行开右侧抽屉~~ ⚠️ 抽屉已被全屏弹层取代（§8.3），点行直接开卡片。
  「复制为表格」出 21 列 TSV，一家公司一行，可直接粘进 Excel / 飞书 / Notion。
  （artifact 沙箱禁止页面发起下载，所以是复制到剪贴板而不是导出文件。）
- **卡片**：自适应网格，每张卡自带全部字段，不需要再点开抽屉；卡片底部
  「在图中查看 →」跳回网络图并聚焦该人。

### 7.4 这轮的关键结构改动 ⚠️（§九已把 `#panel` 整个删除，本节只剩历史意义）

**`#panel` 从 `#stage` 里搬了出来**，现在挂在新的 `.sw` 容器上。
原因：表格/卡片视图下 `#stage` 是 `display:none`，面板留在里面永远量到 `0×0`，
抽屉等于不存在。全屏也随之改成对 `.sw` 调 `requestFullscreen`。
CSS 里 `.sw[data-view="tbl"] .panel:not(.dk)` 负责让空面板在非图视图下不浮在内容上。

### 7.5 数据这轮从哪来

五份报告（四校 AP 扫描 + 北大 310 人全景），现在在
`~/Documents/000_markdown知识库/`（**不在桌面了**）。
原来页面只用到其中「已确认创业」那一层，报告里的**职务邮箱、个人主页、研究方向、
逐轮融资、第二梯队候选**全都没进页面，这轮补上了。

- 226 人 = 已创业 78 · 水下 11 · 待创业 137；高潜力 142；公司 82 · 机构 142
- 原页面 70 人 / 73 条公司记录**一条没丢**（合并时基线优先，脚本有断言）
- **主动丢掉了 368 条**低信号待创业记录（评分 <3 且未标高潜力）。
  那些人的全部信息就是「某 AI 院系里有这么个人」，留着只会淹掉真信号。
  要捞回来得重跑抽取。
- 公司名与机构名做过归一（`穹彻智能` / `穹彻智能 Noematrix` 是同一家，
  `顺为` / `顺为资本` 是同一家），并剔除了 `央企` `车企` `财务投资人`
  这类**报告没点名时的占位词**——它们不是机构，不能变成节点。

### 7.6 新的数据口径坑

- **孙天祥**（日行迹智能）是**上海创智学院** AP，不是复旦教职；
  **周强**（光羽芯辰）是复旦微电子**校友**，不是教职。两条的 `dept` 里已带 ⚠️ 标注。
- **杨耀东 / 洪波 / 谢广明 / 朱松纯 / 仉尚航 / 王启宁** 这批「陷阱人」现在**在库里**，
  但都是 `stealth`，`note` 写明了为什么不算已创业。不要因为看到他们挂着公司名
  就升成 `founded`。
- **谢涛的两校冲突仍未解**（北大讲席教授 vs 清华计算机系名录），现记在北大名下。
- **黄翊东** 报告里是三家公司（华慧芯 / 与光科技 / 光函数科技），页面目前只有与光科技。
- 「创业可能性」评分是抽取时按报告证据打的，**不是官方口径**，页脚已声明。

### 7.7 这轮跑了一遍多视角审查，修掉的东西（别改回去）

审查从五个视角看：交互状态机 / canvas 图层 / 数据完整性 / 无障碍 / 可维护性。
报了 33 项，逐条回代码核实后修了下面这些。**几条容易「顺手改回去」的，标了 ⚠️**：

真 bug：
- **`FIN` 常量是纯死代码**，且键名已与 DB 的公司名对不上。已删除。
  ⚠️ 融资口径现在只有一处：`cos[].fin`。别再建第二张表。
- **同校重名教授会被静默吞掉**：`add()` 撞 id 就返回旧节点，第二个人凭空消失，
  表格里点他打开的是前一个人。现在 id 冲突会加 `#2` 后缀并在自检里点名。
- **`sch` 写错会在自检跑到之前就抛异常**，整页空白而自检永远发不出声。现在先跳过再报错。
- **`Esc` 清空搜索框时表格/卡片不刷新**：上一轮遗留的 `qi` keydown 里写的是
  `runSearch("")`，那只重画网络图。现在统一走 `onQuery()`。
- **切视图不重算搜索态**：图用 `hits` 高亮、表/卡用 `QS` 过滤，是两套东西，
  切过去不同步就会拿上一次查询的结果画图。`setView` 里现在两边都重算。
- **抽屉的关闭按钮会被抹掉**：原来是 `openDock` 事后 `insertAdjacentHTML` 插进去的，
  而 `showPanel` 每次重写 `innerHTML`——面板里点一下跳转，✕ 就没了。
  现在关闭按钮由 `showPanel` 根据 `dock` 一起渲染。
- **`closeDock` 不清 `sel`**，关掉抽屉后表格行还留着选中高亮。
- **赛道筛选条漏了「其他」**，14 位教授筛不出来。⚠️ 筛选条现在**由 `FLD` 生成**，
  往 `FLD` 里加赛道会自动出现在筛选条上，别再手抄一份 chip。
- **「投资机构」chip 在表格/卡片视图里是空操作**（`showInv` 只作用于图），
  却会顺手关掉抽屉。已加 `gonly`，非图视图下隐藏。
- **阿里那条写死的「合并成一个节点，实为两个主体」说明与数据矛盾了**——
  现在阿里巴巴集团和阿里云本来就是两个节点。已删。
- **提示文案说「拖节点可固定位置」，实现是反的**（`pointerup` 里 `pin=0`，松手回弹）。
  文案已改。canvas 的 aria-label 也承诺了「回车查看详情」而 keydown 里没有 Enter 分支，一并改了。
- 三家机构名仍在分裂：`国务院国资委诚通基金`/`诚通基金`、`国家大基金系`/`国家大基金`、
  `靖澄资本「港越一号」`/`靖澄资本`。已并。
  ⚠️ `BV 百度风投`≠`百度`、`联想`/`联想之星`/`联想创投` 是**三个不同主体**，别并。

无障碍：
- `<th role="button">` 会顶掉 columnheader 语义，`aria-sort` 随之失效。
  ⚠️ 排序交互改由 th 内部的 `<button class="sb">` 承载，**别把 role="button" 加回 th 上**。
- `aria-selected` 在普通 `<table>` 的 `<tr>` 上不生效，改用 `aria-current`。
- 行级 keydown 会抢走行内「复制邮箱」按钮和「主页」链接的回车，已加 `closest("button,a")` 放行。
- **`--faint`(#A8ACB1) 对白底只有 2.28:1**，原来用在表头、「待创业」徽标、筛选组标签上。
  ⚠️ 这三处已改 `--muted`。`--faint` 现在只留给装饰性描边与 placeholder，
  **别再拿它写承载信息的小字**。
- 「可能性」刻度条加了 `role="img"`（`aria-label` 挂在裸 span 上会被读屏丢弃）；
  三个计数加了 `aria-live="polite"`。

有意没改的：
- **卡片不整卡可点**。卡面已含全部字段，不需要再开抽屉；跳图走卡片底部那个显式按钮。
  原来 CSS 写着 `cursor:pointer` 却没有 handler，是个假承诺，已把 CSS 删掉而不是补 handler。
- **`CORP` / `STATE` / `SCHOOL_FUND` 仍是三张手工旁表**。它们只影响机构卡片上的
  「产业资本 / 国资 / 校属基金」标签，漏了不影响图和表。加新机构时想标就补一笔。

### 7.8 审查第二轮：修完核实阶段又抓出来的

第一轮报的 33 项里有 22 项经逐行核实为真。下面这批是核实阶段才浮出来的，也都修了。

**跨校机构从 23 变成 24，是修出来的，不是数据变了。**
`北京市人工智能基金` 在报告里有三种写法（另有「产业基金」「产业投资基金」），
建节点时按原始字符串走，被拆成三个单校节点，于是它同时投了交大（连文昭）和清华（翟季冬）
这件事整个从 `CROSS` 统计里漏掉。同类问题还有诚通基金、国家大基金各一组。

⚠️ **归一现在做在派生层的 `IALIAS` 里，不在数据里**。以后再发现同一家的新写法，
往 `IALIAS` 加一行就行，不用回头改 DB 里的每一条 `cos[].inv`。
数据自检会把「互为子串」的机构名列出来提醒你确认——
但 `联想`/`联想之星`/`联想创投` 和 `BV 百度风投`/`百度` 是**真的不同主体，别并**。

**焦点管理（键盘用户原来基本没法用表格）**
- `renderTable()` 用 `innerHTML` 整体重建 tbody，会把当前获得焦点的 `<tr>` 或排序按钮
  连同焦点一起销毁，焦点掉回 `body`。后果：排一次序之后焦点没了，**没法按第二下切升降序**。
  现在渲染前记住焦点在哪（排序键或行 id），渲染后接回去。
- 打开抽屉后焦点留在 body，而抽屉的 ✕ 在 DOM 里排在表格之后，实测是全页第 380 个 Tab 位。
  现在 `openDock` 直接把焦点送到 ✕，`closeDock` 再还给当初那一行。
- 抽屉态下 `#panel` 加 `role="dialog"` + `aria-label="XXX 详情"`，关闭时摘掉。

**`wide()` 和 CSS 断点量的不是同一个东西**
`wide()` 原来是 `W>1040`，而 `W` 是 `#cbox` 的宽度，比视口窄 46px（`.wrap` 的 padding + 边框）。
CSS 那边的断点是 `@media (max-width:1040px)`，用视口宽度。
中间空出 **视口 1041–1086px** 这一段：CSS 认为是宽屏、面板照常浮在画布右上，
JS 却认为是窄屏、`padR()` 只给 22px，于是节点和标签被画到面板底下。
⚠️ 现在 `wide()` 读 `document.documentElement.clientWidth`，**和 CSS 断点对齐**，别再改回用 `W`。
面板宽度也收成一个常量 `PANEL_W`，`padR()` 和标签的 `rightLim` 共用。

**其它**
- 键盘 `+` / `−` 的缩放锚点原来是 `W/2`，那个点在详情面板背后；已和工具条按钮统一成
  `(W-padR()+22)/2`，并补了 `preventDefault()`。
- 搜索把抽屉里那个人筛掉时不再干晾着——和筛选 chip 一个语义，直接关掉抽屉。
- `closeDock()` 现在会 `showPanel(null)` 重画面板。不重画的话，抽屉态渲染出来的 ✕
  会跟着切回网络图，变成一个关不掉的假按钮。
- 数据自检加了**反向检查**：`CORP`/`STATE`/`SCHOOL_FUND` 里登记了但 DB 中根本不存在的机构名
  会被点名（当时报出 非夕科技 / 金山云 / 北大科技园 三条死项，已清）；
  同时补上了漏登记的 `赛灵思`（收购深鉴科技）和 `水木创投`。

**审查报了但【不改】的**
- `canvas` 用 `role="application"` 屏蔽了读屏的浏览模式。改成 `role="img"` 会让方向键导航失效，
  权衡下保留 application，靠 `aria-label` 说明操作方式；表格视图是等价的无障碍替代路径。
- 加载时跑 1200 步 O(N²) 力导向（367 节点 ≈ 8000 万次迭代）。实测首屏没有可感卡顿，
  且布局质量依赖这个步数，先不动。真要动的话是加 Barnes-Hut，不是减步数。

## 八、2026-09-03 第二轮：页面内可增改 + 全屏详情

### 8.1 页面现在能改数据了，但源文件仍是基线

结构是「基线 + 覆盖层」：

- **`DB0`** —— 文件里写死的那份，就是原来的 `DB`，改了名。它永远是基线。
- **覆盖记录** —— 页面里的每一次增改，存成一条 `{kind:"edit"|"add", key, rec}`。
  `edit` 按教授的 `_id` 覆盖整条记录；`add` 是新增的人，id 形如 `N:<uid>`，与姓名无关。
- **`DB`** —— 有效集合 = `DB0` + 覆盖，由 `overlayAndRebuild()` 现算，然后 `build()` 重建图。

所以「重置为原始」永远可用（删掉那条覆盖记录即可），「导出修改」导出来的 JSON
只有改动的部分，不会把整份数据抄一遍。要把改动固化进源文件，把导出的 JSON 发给 Claude 合并，
或者用 Artifact 工具的 `read_db` 直接读 `profs` 集合。

⚠️ **别在 `DB0` 里手改一条已经被页面覆盖过的记录**——覆盖是整条替换，源文件里的改动会被盖掉。
先「重置为原始」再改源文件，或者反过来只在页面里改。

### 8.2 存储后端

`store.init()` 先试 `claude.use("db")`（artifact 的 db 能力，发布时声明 `capabilities:{db:{}}`）：
- 拿到了 → `mode:"db"`，覆盖记录存在 claude.ai，**同组织内所有打开这个页面的人共享**，
  `onSnapshot` 实时同步。工具条右侧显示「已同步 · N 处修改」。
- 拿不到（本地打开、或没授权）→ `mode:"local"`，退回 `localStorage`，只在这个浏览器里有效，
  工具条显示红色「仅存本浏览器」。
- 云端写失败会 toast 出错误码并暂存本地，不会静默丢。

⚠️ 声明了 `db` 能力的 artifact **不能公开分享**（只能组织内查看）。这是平台规则，不是页面的选择。
⚠️ 文档 id 只能用 ASCII（路径语法限制），所以 `edit` 记录的 id 是 `_id` 的 FNV 哈希（`hid()`），
`add` 记录的 id 是 `"n"+uid`。真相在 body 的 `key`/`uid` 字段里，别拿文档 id 反推。

### 8.3 全屏详情弹层（替代了原来的右侧抽屉）

`#mo`：`position:fixed` 全屏；宽屏时居中一张 820px 的卡，窄屏铺满。
三个入口：表格点行、卡片点整卡、网络图详情面板右上的「展开 ⤢」。
- 头部固定，正文 `.mo-bd` 独立滚动，`overscroll-behavior:contain`——滚到底不会带着页面跑。
- 打开时 `body.mo-open` 锁页面滚动；焦点送到 ✕，Tab 在弹层内循环；关闭后焦点回到打开它的那一行/卡片
  （按 `data-p` 重新找，因为保存会重渲染表格，原来的元素早没了）。
- Esc 关闭；编辑态下 Esc 会先问是否放弃修改；`⌘/Ctrl+Enter` 保存。
- 打开动画用「强制回流 + 加 class」，**不用 rAF**——后台标签页里 rAF 会被节流，弹层会卡在透明态。

**快速标签行**（查看态顶部）：状态三选一、高潜力开关、可能性 1–5，**点一下即保存**。
两条口径守卫在这里也生效：标「创业项目」必须已有公司；挂着公司的人不能标「待创业」（提示改「水下项目」）。

**编辑态**：全字段表单。基线记录的姓名和学校不可改（它们构成 `_id`）；新增的人可以。
公司列表可增删，投资方用「、」或逗号分隔。保存前跑 `checkRec()`，和控制台自检同一套规则。

### 8.4 网络图详情面板 ⚠️（§九已删侧栏，`#pex`/`#pcl` 不复存在）

面板不再有抽屉态。教授面板右上多了「展开 ⤢」和「✕」（`#pex` / `#pcl`）。
`#pcl` 只是清掉选中，不影响任何数据。

### 8.5 重建时的布局保持 ⚠️（§十六改为分层图后此机制重写：`rebuild()` 记下当前层各节点坐标传给 `layout(prev)`，老节点不动、新节点补位）

`layout(prev)`：传入上一次各节点坐标，老节点原地不动、只给新节点找位置并跑 160 步；
`fresh=0`（比如只改了个标签）就一步都不跑。首次加载仍是 1200 步。
用户缩放过的视图（`userView`）在重建后不会被 `fit()` 打回去。

### 8.6 手机

`@media (max-width:1040px)` 下 `#net{touch-action:pan-y}`——竖向滑动交给页面滚动，
否则画布会把整屏的竖向手势吃掉，用户卡在图上翻不下去。代价是手机上不能用手指竖向平移图，
横向和捏合仍归画布。

### 8.7 表格 / 卡片上的「已修改」标记

被页面覆盖过的记录：表格行姓名后有小字「已修改」（`tr[data-mod]`），卡片右上角同样。
弹层查看态也会显示「已修改」徽标。

### 8.8 快速标签的两条小规则

- 已创业（`founded`）的「可能性」刻度段是**禁用**的，固定显示 5——评分是「创业可能性」，
  已经创业的人这个维度没有意义。编辑态的下拉框里也写着「已创业固定 5」。
- 「导出修改」在没有任何覆盖记录时只 toast 一句，不会复制一个空 JSON。

### 8.9 怎么把页面里的改动合回源文件

两条路，都不需要手抄：
1. 页面上点「导出修改」→ 剪贴板里是 `{edits:{<docid>:{kind,key|uid,rec}}}`，发给 Claude，
   让它按 `key` 把 `rec` 合进 `DB0`、把 `add` 追加进去，然后重新发布。
2. Claude 直接用 Artifact 工具 `read_db`（`collection: "profs"`）读同一批记录，效果一样。

合并完记得清掉 db 里对应的记录（`write_db` 的 `delete`），否则页面会把已经固化的改动再覆盖一遍——
虽然结果一样，但「已修改」标记会一直挂着。

## 九、2026-09-03 第三轮：去掉侧栏与介绍，页面改叫「清北复交 AP Network」

用户明确要求：**网络图不要侧栏**，点教授直接全屏弹出卡片；页头那段介绍不要；标题改成
「清北复交 AP Network」。都照做了，几个连带改动记一下：

- `#panel` 及其全部 CSS 已删。`showPanel()` 留作**空函数**，只是免得改十几个调用点；
  选中态（`sel`）只画在图上（外圈）。⚠️ 别往 `showPanel` 里塞东西。
- **点击 vs 拖动**由 `press` 区分：按下记起点，移动超过 6px 算拖动；松手时没拖动 → `openNode(n)`。
  所以拖节点仍然是拖，轻点才开卡片。手机上手指抖一下也算拖，阈值别调太小。
- `openNode(n)` 对任何图节点都有效：教授走 `openModal(d)`；**机构 / 学校**也开同一个弹层，
  正文由 `nodeBody(n)` 渲染（就是原侧栏里那两段：投过谁、各状态人数）。
  这类弹层没有编辑与快速标签（`MO.node` 非空时 `renderModal` 直接短路）。
- 弹层里教授名可点（`data-go`），点了关弹层并在图上定位到他。
- 画布键盘：**回车**打开选中节点的卡片，Esc 取消选中；aria-label 已同步。
- 侧栏没了，`padR()` 固定 22，图占满整个画布宽度；`wide()` 还留着但只有 `fit()` 的左 padding 在用。
- `<title>` 与 `<h1>` 都是「清北复交 AP Network」，h1 里四个字按各自院校色着色
  （清=紫 北=青 复=蓝 交=红，沿用既有的 `.t .p .f .s`）。
  `#find` 段落和 `.kick` 都删了；`renderStats()` 不再写 `#find`。顶部只剩八个数字。

## 十、2026-09-03 第四轮：三个默认值

用户要求，都是有意的默认，别改回去：

1. **打开默认是卡片视图**（`setView("card")`），视图切换按钮的顺序也改成 卡片 / 表格 / 网络图。
   首屏 `#stage` 是隐藏的，`resize()` 的退化尺寸守卫会拒掉这次量到的 0，
   切到网络图时 `setView("net")` 会再量一次并 `fit()`，不用额外处理。
2. **网络图里投资机构默认不显示**（`showInv=false`，chip 初始 `aria-pressed="false"`）。
   「重置」也回到关；想看机构点一下 chip。数据侧照常建机构节点，只是不画。
3. **顶部八个数字的统计栏删了**（`#nums` 与 `.nums/.num` CSS 一并删除，`renderStats()` 只写页脚）。
   页头现在只有一行标题。计数在页脚还有。

## 十一、2026-09-03 收尾：db 版已上线

用户关掉了公开分享，页面以 `capabilities:{db:{}}` 重新发布（runtime contract 0.2.39，sharing = owner）。
现在：
- 页面内的增改存在 claude.ai 的 artifact db，集合名 `profs`；同组织内打开页面的人实时共享。
- 工具条右侧应显示「已同步」；若显示「仅存本浏览器」说明这次打开没拿到 db（本地文件、或未授权）。
- **Claude 读回改动**：Artifact 工具 `read_db`，`collection: "profs"`，`db_op: "list"`。
  每条 body 是 `{kind:"edit"|"add", key|uid, at, rec}`，合回 `DB0` 后用 `write_db` 的 `delete` 清掉。
- ⚠️ 只要 `db` 还在声明里，这个 artifact 就**不能再开公开分享**；要公开就得去掉 `db`
  （发布时传 `capabilities:{}`），页面会自动退回本浏览器存储。

## 十二、2026-09-03 打包：文件夹改名 QBFJ AP Network

- 页面源码与本说明搬到 `~/Desktop/QBFJ AP Network/`，HTML 改名 `QBFJ AP Network.html`。
  发布时**必须继续传 `url` 参数**，否则新路径会创建一个新 artifact。
- `db/profs.json` 是 claude.ai 上 db 的**快照**，不会自动更新；`db/README.md` 写了怎么刷新、怎么合回源文件。
- **db 本身不在本地**，也放不进文件夹——它只存在于 claude.ai，页面和 Claude 才能读写。
- 另外三个页面（交大复旦图谱 / 清华管道 / 北大闭环）留在旧文件夹 `四校教授扫描_源码/`。

## 十三、贴新闻自动更新

用户问「刷到 AP 创业新闻贴给你能不能直接更新网页」——能，走 db 覆盖层，不碰源文件、不重发布。
工具是 `db/merge_news.py`，用法与合并规则见 `db/README.md`。

Claude 侧的固定动作：
1. 从新闻里认出教授（姓名 + 学校）；对不上 `DB0` 里的人就按新人处理（`kind:"add"`）。
2. 抽字段写 patch：公司名、角色、轮次、金额与领投方（进 `fin`）、投资方列表、日期、来源。
   **新闻没说的字段别猜**，patch 里不写就不会动。
3. `read_db get` 看这个人有没有已存在的覆盖；有就 `--current` 传给脚本。
4. 跑脚本 → 拿到 `{collection, doc_id, data}` → `write_db set`。
5. 回复里说清改了哪几个字段；口径存疑的（传闻、未官宣、同名）写进 `note` 而不是当事实。

⚠️ 别用 `write_db update` 局部改 `rec`——`rec.cos` 是数组，`update` 会整段替换，等于没合并。一律 `set` 整条。

## 十四、2026-09-03 第五轮：四态口径 + 全量名录

用户重新定义了状态，并要求把 AI 相关学院的教授/AP **全部**收进来（不再按信号筛）。

### 14.1 四态（`STAT`，页脚也写了）
- `growth` **成长期项目**：B 轮以后（B+/C/D/IPO/被收购）
- `founded` **创业项目**：B 轮及之前（含 B 轮，B1/B2 视为 B 轮阶段）且有融资新闻
- `stealth` **水下项目**：有项目但无融资新闻——**用户手动补**
- `potential` **待创业**：尚未创业

`HASCO(st)` = growth 或 founded，代码里凡是「已创业」的判断都走它。图上成长期 = 实心 + 外圈。

**重分时的显式判定**（都写进了各人 `note`）：商汤医疗/张少霆 → 成长期（分拆后半年融 10 亿，报告无轮次）；
川土/陈东坡 → 成长期（9 轮）；小马智行/楼天城 → 成长期（**据公开信息补的纳斯达克上市，报告未载，请核**）；
智元/闫维新、星动/陈建宇、银河通用/王鹤 → 成长期（B 轮后战略轮或累计规模）；
朱芳芳、张钹、朱军、金贤敏 → 仍创业项目（B/B1/B2）；途深智合/王宇光、不筹量子/李晓鹏、上纬启元/董豪 → 水下（无融资新闻）。

### 14.2 高潜力策略
**全部清零**（原来 142 个是抽取 agent 的判断，用户不要）。只按 md 明确标注预标 64 人：
四校报告 §3「第二梯队」+ 北大全景 S 档 / A 档。其中 S 档含已创业者（王鹤、董豪、卢宗青、杨耀东），
是 md 原文的标注，用户可在卡片上一键取消。

### 14.3 全量名录并入（`db/rebuild_db0.py`）

⚠️ 2026-09-04 起不再批量导入，`rebuild_db0.py` 已从仓库删除（§二十一）；本节与 §18.2 中关于它的内容仅作历史。
从五份 md 解析名录（表格 + 顿号名单），**确定性解析、无 LLM**：
交大 AI 学院 / 计算机学院各所 / 自动化与感知学院（约 200 人名单）；复旦 TEAI / 计算与智能创新学院 / 大数据学院；
清华 IIIS / AI 学院全职 PI 与核心领域兼聘 / AIR / 计算机系各所；北大 CFCS / AI 研究院各中心 / 全景 17 个单位 /
交叉院系里所在院系为信息·计算·智能·电子·软件·工学的 / 软微工学博导。
排除：兼职、访问、AI+X 交叉院系里的非 AI 院系、软微的跨聘博导与硕导。
结果：226 → **960 人**（成长期 19 / 创业 56 / 水下 14 / 待创业 871）。
名录只给姓名的（约 530 人）`research` 是占位「—（名录未给研究方向，待补）」，`title` 是「教师（名录未给职称）」——
不是漏，是名录本身没有。

⚠️ 重跑 `rebuild_db0.py` 是安全的：已有的人只补空字段不覆盖；但**状态重分与高潜力清零会再执行一次**，
如果用户已在页面里手动改了状态/高潜力，那些改动在 db 覆盖层里、不受影响；源文件 DB0 里的会被重算。

### 14.4 布局改懒加载 ⚠️（已被 §十六 分层图取代：不再有全图，`LAID` 现在表示「当前层级已布局」）
960 人后 O(N²)×1200 步的力导向会卡首屏。现在 `layout()` 只在**第一次切到网络图**时跑（`LAID` 标志），
且节点 >700 时只跑 420 步。卡片是默认视图，所以打开页面不再等布局。

## 十五、2026-09-03 第六轮（数据部分）：组织谱系 + 交大个人页抓取

### 15.1 三个脚本的分工（都在 `db/`，都可重跑）
| 脚本 | 做什么 | 幂等性 |
|---|---|---|
| `rebuild_db0.py` | 从五份 md 解析名录 → 并入 DB0；解析时把「学院 · 研究所」拆成 `dept` + `grp`；已有的人只补空字段 | 状态重分与高潜力清零**会再执行** |
| `enrich_org.py` | 规范 `dept`（别名表、剥掉头衔括号）、补 `grp`、挂 `lead`（院长/所长） | dept 已干净时**保留原有 grp**（第一版不幂等，把 555 个 grp 删了一次，已修） |
| `scrape_sjtu.py` | 交大计算机学院 / 自动化与感知学院个人页（URL 有固定规律：`jiaoshiml/{拼音}.html`、`faculty/{拼音}.html`）抓 职称/邮箱/研究方向；页面缓存在 `db/.cache/` | 只填空字段 |

**顺序必须是 rebuild → enrich → scrape**。三步跑完：1139 人，grp 548 / lead 472，研究方向占位从 530 降到 393，无邮箱 426。
其他三校个人页没有可推的 URL 规律，没抓；要补得逐人开页面。

### 15.2 新字段
- `grp`：研究所 / 中心 / 实验室（学院下一级）。50 个，最大的是交大计算机学院的各所（网络空间安全 29、信安密码 23…）。
- `lead`：该学院或研究所的院长 / 所长，用于卡片上的「所属」行。来源：报告标题里写明的（IIIS/AI 学院 姚期智、AIR 张亚勤·刘洋、北大智能学院/AI 研究院 朱松纯、CFCS 高文·Hopcroft、交大 AI 学院 王延峰·张娅、复旦 TEAI 姜育刚、大数据/类脑 冯建峰、机器人研究院 甘中学、通用物理智能 苏昊）+ 交大计算机学院各所的所长（从名录 title 含「所长」的人反推）。
  **不是导师-学生关系**，是组织归属；卡片上叫「所属 / 同所 / 同院」，别写成「师徒」。

### 15.3 仍不干净的 dept（数量很小，先留着）
`类脑计算研究中心（关联`、`上海创智学院（⚠️非复旦教职`、`深圳研究生院 信息工程学院（大表列为…` 这类是基线里的自由文本，各 1 人，会成为图上的单人学院节点。要清就在 DB0 里手改 `dept`。

## 十六、2026-09-03 第六轮（图部分）：分层网络图

用户要求：不要一张全图（上千个点会卡），改成 **学校 → 学院 → 研究所 / AP** 逐层展开；卡片里加组织谱系。

### 16.1 三层
- **L0 四校**：4 个学校节点，锚在一条横轴上（`SPINE`），点学校进入 L1。
- **L1 某校的学院**：学校为中心 hub，学院节点围绕，圆里写人数，标签下一行是「N 人 · 创业 x · 高潜 y」。点学院进入 L2。
- **L2 某学院**：研究所 / 中心为 hub（横向排开，`anchor` 等距），AP 挂在各自 hub 下；没有研究所的学院只有一个 hub。
  点 AP 开卡片；点研究所开成员卡（含所长、按状态标记）；投资机构只在这一层、且开关打开时才画。

`G={sch,dept}` 是当前层级；`buildGraph()` 按层级现建 `N/L/byId`，`layout()` 只对这一层跑力导向。
**布局的两条规则（改过坑）**：① L2 的研究所 hub 排成网格（列数 ≈ √(1.7k)），不是一整行——20 个 hub 排一行会变成 7000 宽的细条，`fit()` 之后全是芝麻点；② 步数按 N² 预算 `min(1200, 2.2e7/N²)`，最少 180 步并用 alpha 衰减收住——315 点跑满 1200 步实测 4.7s。现在最大的一层（交大计算机学院 315 点 / 20 所）浏览器实测约 1s（含首次画布文字测量），其余学院 <0.1s，且只在钻入那一层时算一次。
`drillTo(sch,dept)` 是唯一的换层入口：它会同步学校 chip、学院下拉、面包屑，并重建重排。
`goToId(id)`：按 id 定位任何对象（教授 / 机构 / 学校 / 学院），需要就先钻到对应层再聚焦——弹层里所有 `data-go` 链接走它。

### 16.2 导航
面包屑 `#crumb` 在画布左上（四校 › 清华大学 › 交叉信息研究院 IIIS，可点回上层）；`Backspace` 上一级；`Esc` 先取消选中、再上一级。
学校 chip 在图视图里 = 钻到该校 L1；学院下拉 `#deptsel`（选了学校才出现）= 钻到 L2。
这两个筛选**三视图通用**：卡片 / 表格也按 `fDept` 过滤（`matchProf`）。

### 16.3 卡片上的组织谱系
- 「所属」行：学校 › 学院 › 研究所，下一行是院长 / 所长（`lead`）。学校、学院可点，会切到图并钻到那一层。
- 「同所」/「同院」行：同研究所（没有研究所则同学院）的其他人，点名字直接换成那个人的卡片（`data-open`），最多列 40 人。
- 这是**组织归属**，不是导师-学生关系；报告里没有师承数据。

### 16.4 图上的编码（沿用 + 新增）
学校 / 学院实心院校色；研究所空心 + 中心点；AP 四态同前；**高潜力 AP 右上角一个小黑点**；机构沙色。
人多时（>60 点）待创业且非高潜的 AP 不标名，放大或悬停才显示——否则交大计算机学院那层全是字。

### 16.5 本地预览的坑
`~/Desktop` 受 macOS 隐私保护，Claude 的预览进程读不到，`file://` 打不开、本地 http.server 也 404。
本地验证要把 HTML 拷到 scratchpad 再起服务（`~/.claude/launch.json` 的 `qbfj` 配置指向那里）。

## 十七、2026-09-04：剔除交大自动化与感知学院

用户决定：创业命中率太低，整院去掉。做法：
- DB0 里该院 **待创业** 的 200 人全部删除；**王贺升**（UniX AI 优理奇首席科学家，已创业）保留，他的 `dept` 仍是「自动化与感知学院」，图上会是交大下面一个只有他的学院节点——这是刻意的，不是漏删。
- `rebuild_db0.py` 不再解析该院名录（`EXCLUDE_DEPT`），`scrape_sjtu.py` 去掉了该院的 URL 规律。**重跑脚本不会把他们加回来。**
- 要恢复：把 `EXCLUDE_DEPT` 清空并把 §6 自动化的解析段落从 git/备份里找回（这轮直接删了）。

## 十八、2026-09-04：用户手动改的一律照用户的来

用户明确定的规则：**页面上手动改过的记录，脚本和 Claude 都不得回头「纠正」。**
起因是我拿轮次口径质疑了他把卢策吾标成成长期。以后不质疑，照用。

这条不是靠记性守的，已经做成强制约束：

### 18.1 `manual` 标记
页面里任何一次保存（编辑表单 `saveDraft` / 快速标签 `quickSet`）都会给该条记录盖上 `manual:1`。
它跟着覆盖记录一起存进 db，也进 `EDITABLE` / `cleanRec` 的白名单。

### 18.2 三处保护
⚠️ `rebuild_db0.py` 已删除（§二十一），下表前两行成为历史；现在唯一会改数据的脚本是 `merge_news.py`，规则仍是第三行。

| 位置 | 原来会做什么 | 现在 |
|---|---|---|
| `rebuild_db0.py` 第 ~179 行 | 按轮次重算 founded → growth/stealth | `if d.get("manual"): continue` |
| `rebuild_db0.py` 第 ~189 行 | `d["hot"]=0` 把**所有**高潜力清零再按 md 重标 | manual 记录跳过，保留用户标注 |
| `merge_news.py` `merge()` | patch 里的 status / hot 直接覆盖 | `hot` 永不由脚本写；manual 记录的 `status` 要显式 `--force-status`，且会打印一行提示 |

`merge_news.py` 里 `USER_OWNED={"hot","manual"}`——**贴新闻更新时，高潜力永远不动**，那一列完全归用户。

### 18.3 已回填
`张牧涵`(p683511ff) 与 `卢策吾`(pb9c9de6) 这两条是用户手动改的，已补 `manual:1`。
`兰艳艳` / `张亚勤` 是我从新闻入的库，不算手动编辑，没标——将来用户在页面上动它们时会自动打标。

### 18.4 合并回 DB0 时的硬规则（保护的真正落点）

`rebuild_db0.py` 只动 `DB0`，**不碰 db 覆盖层**——所以用户在页面上的编辑在运行时永远赢，
跟有没有 `manual` 标记无关。`manual` 唯一起作用的时刻是：**把覆盖合并进 `DB0` 之后再跑脚本**。

因此合并回 `DB0` 的人（通常是 Claude）必须做一件事：

> **凡是来自 db 覆盖层的记录，写进 `DB0` 时一律带上 `"manual":1`。**
> 它们按定义就是人手改的，不需要判断，也不要遗漏。

做到这一条，即使页面没有自动打标，保护也是完整的。页面里的自动打标（`saveDraft` /
`quickSet` 写 `d.manual=1`）只是让新产生的覆盖记录自带这个字段，省掉合并时的一步，
**不是保护的必要条件**。

## 十九、2026-09-04 清理：冗余、冲突与两个 bug

用户要求检查代码/脚本/文档的冗余与冲突。做了这些，都已提交：

**页面**
- 删掉死代码：`showPanel()` 空桩及两处调用、从未被调用的 `resetFilters()`、已删元素的 CSS（`.kick` `.find` `.stripe`）、CSS 里一个孤立的 `}`。
- 四处与实现矛盾的注释改正（「故意没有整卡点击」「抽屉」「面板挂在 .sw」）；DB0 字段文档补上四态与 `manual`。
- **bug 1**：`pointercancel`（触屏被系统接管、滚动打断）也走 `endPointer`，会当成点击打开卡片。现在 cancel 不开卡片。
- **bug 2**：每次快速标签保存都 `rebuild()` → 整层力导向重跑（交大计算机学院约 1s，且节点全部换位）。
  现在 `rebuild()` 把当前层坐标传给 `layout(prev)`，老节点不动、只给新节点补位，实测 0ms。
- 加载时空覆盖集也会触发一次多余重建（自检因此打印两遍）：`store.sig` 初值改为空集签名。
- 从卡片/表格跳图（`goToId`）原来会先按旧层级建一次图再钻一次，现在直接定好层级再进图。

**脚本**
- `enrich_org.py` 与 `scrape_sjtu.py` 的 `ORDER` 缺 `manual`——重写 DB0 时会把这个字段**静默丢掉**，保护随之失效。已补。
- `scrape_sjtu.py` 解析器：原来把「研究方向为 X」抓成「为 X」，且不截页脚（二维码 / 学院地址 / 招生 / 工作履历），
  110+ 条研究方向带噪音。现在有 `clean_research()`，并加 `--refresh` 用缓存页重新解析已抓过的人（不上网，`manual` 记录不动）。
  本轮跑过：见 git log 里的数字。
- `merge_news.py` 顶部示例仍写 `hot` 为可选输入，已改为「hot 归用户，写了也忽略」。

**文档**
- 交接说明顶部加「当前状态一览」；§〇 字段表补四态 / `grp` `lead` / `manual`；
  §四 §五 §六.5 §七.3 §七.4 §八.4 §八.5 §十四.4 八处被后来推翻的说法就地标 ⚠️，历史不删。
- `db/README.md` 补 hot/manual 规则与 `--refresh`。

**没动的**
- `~/Desktop/四校教授扫描_源码/` 里有 `probe_250.html` `probe_1200.html`（审查 agent 留下的性能探针）和 `北京大学师资全景.html`，
  不在本项目目录、来源不明确，没有删，用户自行处理。
- 页面这次的改动当时尚未发布（线上有另一个会话 04:33 的版本，覆盖前平台要求逐行读完）——已随 §二十 一起发布。

## 二十、2026-09-04 发布 + 合回覆盖层 + 括号残渣

**发布**：§十九 的页面改动已随本轮一起发布到同一链接（版本标签「manual 保护 + 合回 db + 括号清理」），`db` 能力沿用。

**合回 db 覆盖层（§18.4 第一次实战）**
- 线上 `profs` 有 4 条：`兰艳艳` `张亚勤`（add，艾斐智药 AIPher）、`张牧涵` `卢策吾`（edit）。
  兰艳艳 / 张亚勤 在 §十四 名录导入后**同时**存在于 DB0（清华人工智能学院兼聘 PI，待创业）——页面上一个人出现两次。
- 处理：4 条全部按 §18.4 合进 DB0 并带 `manual:1`。兰艳艳 / 张亚勤 的 DB0 行改挂 AIR（职称 / 公司 / 来源取 add 记录，兼聘 PI 身份并入 honor；兰艳艳挂 AIR 的 lead，张亚勤本人是创始院长不挂）。
  张牧涵的 edit 里 `dept` 还是拆分前的合写形式「人工智能研究院 机器学习与智能科学理论研究中心」，合并时保留 DB0 已拆好的 dept / grp / lead，其余字段照 edit。
- 发布成功后才 `write_db` 批量 delete 这 4 条（先删后发会让线上页面短暂丢掉这些改动），db 现为空。
  合并前快照存档 `db/merged/2026-09-04_profs.json`；`db/profs.json` 刷新为空集。
- 顺带消掉一个隐患：`EDITABLE` 现含 `grp / lead / manual`，而旧 edit 文档没有这三个字段，覆盖时 `delete t[k]` 会把它们从有效记录上抹掉。合回后不再有旧文档；今后页面保存的记录自带这三个字段。

**括号残渣（数据 bug，线上老版本就有）**
- 20 条清华人工智能学院兼聘 PI 的 `grp` 是「核心领域）」。根因在 `rebuild_db0.py`：名录 dept「人工智能学院（兼聘 PI · 核心领域）」被先按「 · 」切开，
  `enrich_org.py` 的括号优先拆分根本没机会看到原串，幂等路径（dept 已干净就保留 grp）又把坏 grp 保住了。
  现在 `rebuild_db0.py` 有 `dsplit()`（括号里带「·」的整段当分组），三处调用点统一走它；DB0 里 18 条改为「兼聘 PI · 核心领域」（另 2 条即兰艳艳 / 张亚勤，已改挂 AIR）。
- 朱松纯 / 王启宁 的 `grp` 是老拆分器留下的「院长）、人工智能研究院（院长」之类，删掉（身份在 honor / lead 里）。
  孙天祥 / 裴京 / 袁粒 的 `dept` 被截断在左括号处，分别改为「上海创智学院（非复旦教职）」「类脑计算研究中心」「深圳研究生院 信息工程学院」。
- 复核：DB0 939 条无不配对括号；node 桩执行 script 1–3，自检全过（公司 83 · 机构 138 · 跨校机构 24）。

**用户待办**：`git push`（§十九 与本轮的提交都还在本地）。

## 二十一、2026-09-04 定：不再批量导入，`rebuild_db0.py` 退役

用户决定以后只用两种方式维护数据：**页面里手改**（保存即进 db 覆盖层，自动 `manual:1`），
或**贴新闻给 Claude**（`db/merge_news.py` 生成完整记录 → `write_db`）。不再从四校名录 md 批量导入。

因此：
- `db/rebuild_db0.py` 已从仓库删除。最后版本在提交 `08a4937`，`git show 08a4937:db/rebuild_db0.py` 能拿回来。§十四.3、§十八.2 里关于它的说明只作历史。
- 桌面 `~/Desktop/四校教授扫描_源码/` 里的五份 md 副本从此没有任何脚本读（原稿在 `~/Documents/000_markdown知识库/`）。
  那个文件夹用户选择暂不动；里面另有三个独立 artifact（交大复旦图谱 / 清华管道 / 北大闭环）的源码，以及两个可删的性能探针 `probe_*.html` 和来源不明的 `北京大学师资全景.html`。
- 留下的三个脚本：`merge_news.py`（贴新闻主力）；`enrich_org.py`（院长 / 所长对照表 `LEADS` 在里面，把页面新增的人合回 DB0 后跑一遍可自动补 `dept / grp / lead`，幂等）；
  `scrape_sjtu.py`（可选，只在想给交大计算机学院的人补主页 / 邮箱 / 研究方向时用）。
- 网站运行不依赖以上任何脚本或 md：DB0 写在页面里，自包含。

**新增一个人的路径**：页面上「＋ 添加」，或把新闻贴给 Claude。两种都先落在 db 覆盖层；攒够一批后按 §18.4 合回 DB0 并清空 db（先发布、再删文档）。

## 二十二、2026-09-04 流程固化：merge_back.py · find.py · 两个 skill

用户定的三种维护方式（页面手改 / 贴新闻 / 扫官网名录）里，先把前两种和「合回」做成可重复的东西：
- `db/merge_back.py`：db 快照 → DB0，规则就是 §二十 那次手工合回的规则（edit 以表单字段为准、add 撞同名并入、
  一律 `manual:1`、dept 变了交给 enrich_org 重挂），带 `--dry-run` / `--clear-snapshot`，会打印要删的文档清单。
- `db/find.py`：查人（DB0 行、`_id`、edit 文档 id、快照覆盖、同名提醒）、查投资机构（是否登记标签、相近写法）。
- `~/.claude/skills/qbfj-news/SKILL.md`：贴新闻的完整步骤与口径；`~/.claude/skills/qbfj-sync/SKILL.md`：合回 + 发布的顺序。
  两个 skill 只在这台机器的 Claude Code 里，不在仓库；仓库里的 `db/README.md` 有同样的内容。
- 扫官网名录（第三种）尚未做；探测结果：清华计算机系 / IIIS、北大 AI 研究院 / CFCS / 信科 / 智能学院 / 计算机学院、复旦大数据学院
  的名录页是服务端渲染可以直接抓；交大计算机学院与 AI 学院、清华 AIR、复旦计算与智能创新学院 / AI / faculty 门户是 JS 渲染，要走浏览器。
